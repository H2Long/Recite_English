# 基于 C 语言的跨平台智能英语单词记忆系统 —— 课程设计报告

---

## 摘要

本文阐述了一款面向英语学习者的桌面级单词记忆软件的设计与实现。系统采用 **C11 标准**与 **raylib 多媒体图形库**构建，通过**即时模式图形用户界面（Immediate Mode GUI）**范式，在 Linux、Windows 及 macOS 平台实现一致的交互体验。系统核心包含用户账户管理、单词库维护、多模式智能复习、自适应学习计划及跨语言文字渲染五大子系统。设计上遵循**全局单例状态管理**与**函数指针驱动导航**的架构模式，以纯文本管道分隔文件实现轻量级数据持久化，不依赖外部数据库引擎，真正做到零配置、开箱即用。

**关键词：** 即时模式 GUI；间隔重复；C11；跨平台图形应用；自适应学习系统

---

## 一、系统总体架构

### 1.1 架构概览

系统采用 **分层单向依赖** 架构，自底向上分为数据层、业务逻辑层、UI 基础组件层与页面展示层。各层之间通过全局单例状态对象 `AppState` 进行数据共享，避免跨层指针传递。

```mermaid
graph TB
    subgraph 页面展示层
        A1[主页仪表盘]
        A2[浏览学习]
        A3[卡片背单词]
        A4[选词背单词]
        A5[测试模式]
        A6[查找单词]
        A7[学习计划]
        A8[系统设置]
        A9[账号管理]
        A10[词库管理]
    end

    subgraph UI基础组件层
        B1[按钮/复选框/文本框]
        B2[滚动视图]
        B3[搜索栏]
        B4[单词卡片/抽认卡]
        B5[多项选择题]
        B6[跨文字渲染引擎]
    end

    subgraph 业务逻辑层
        C1[账户系统]
        C2[单词库引擎]
        C3[学习计划引擎]
        C4[搜索匹配引擎]
        C5[进度追踪]
    end

    subgraph 数据层
        D1[accounts.txt]
        D2[words.txt]
        D3[progress.txt]
        D4[plans.txt]
    end

    subgraph 核心框架
        E1[全局状态管理器]
        E2[树形菜单导航]
        E3[主循环调度器]
    end

    A1 --> B1 & B2 & B4
    A2 --> B1 & B2 & B4 & B6
    A3 --> B1 & B4 & B6
    A4 --> B1 & B5 & B6
    A5 --> B1 & B5 & B6
    A6 --> B1 & B2 & B3 & B4 & B6
    A7 --> B1 & B2
    A8 --> B1 & B2
    A9 --> B1 & B2
    A10 --> B1 & B2 & B3

    B1 & B2 & B3 & B4 & B5 --> C1 & C2 & C3 & C4 & C5
    C1 & C2 & C3 & C5 --> D1 & D2 & D3 & D4
    C1 & C2 & C3 --> E1
    A1 & A2 & A3 & A4 & A5 & A6 & A7 & A8 & A9 & A10 --> E2
    E1 & E2 --> E3
```

### 1.2 系统启动流程

```mermaid
flowchart TD
    Start([程序启动]) --> InitWindow[初始化图形窗口<br/>1600x1000 60FPS]
    InitWindow --> InitState[AppState初始化<br/>全局状态归零]
    InitState --> LoadWords[加载单词库<br/>words.txt]
    LoadWords --> LoadAccount[加载账户系统<br/>accounts.txt]
    LoadAccount --> LoadPlan[加载学习计划<br/>plans.txt]
    LoadPlan --> InitProgress[初始化进度数组<br/>progress.txt]
    InitProgress --> LoadFonts[加载多语言文字引擎<br/>中文字体/英文字体/IPA音标字体]
    LoadFonts --> BuildMenu[构建树形菜单<br/>7个顶级节点 3个子节点]
    BuildMenu --> RefreshList[刷新待复习单词列表]
    RefreshList --> MainLoop[进入主循环]

    MainLoop --> BeginFrame[UIBegin<br/>更新输入状态]
    BeginFrame --> ClearBG[清屏绘制背景]
    ClearBG --> DrawTopBar[绘制顶栏<br/>用户信息按钮]
    DrawTopBar --> DrawNav[绘制左侧导航树]
    DrawNav --> DrawPage[调用当前页面函数<br/>CURRENT_MENU->show]
    DrawPage --> LoginOverlay{需要登录?}
    LoginOverlay -->|是| DrawLogin[绘制登录覆盖层]
    LoginOverlay -->|否| EndFrame[EndDrawing / UIEnd]
    DrawLogin --> EndFrame
    EndFrame --> CheckQuit{窗口关闭?}
    CheckQuit -->|否| MainLoop
    CheckQuit -->|是| Cleanup[释放资源<br/>关闭窗口]
    Cleanup --> Stop([程序退出])
```

### 1.3 模块依赖关系

```mermaid
graph LR
    main.c --> core/app_state.c
    main.c --> modules/words.c
    main.c --> modules/account.c
    main.c --> modules/plan.c
    main.c --> modules/fonts.c
    main.c --> ui/menu_callbacks.c

    core/app_state.c --> modules/account.c
    core/app_state.c --> modules/plan.c
    core/tree_menu.c --> core/app_state.c

    ui/menu_callbacks.c --> core/tree_menu.c
    ui/menu_callbacks.c --> ui/pages/page_home.c
    ui/menu_callbacks.c --> ui/pages/page_learn.c
    ui/menu_callbacks.c --> ui/pages/page_review_root.c
    ui/menu_callbacks.c --> ui/pages/page_card_review.c
    ui/menu_callbacks.c --> ui/pages/page_select_word.c
    ui/menu_callbacks.c --> ui/pages/page_test.c
    ui/menu_callbacks.c --> ui/pages/page_search.c
    ui/menu_callbacks.c --> ui/pages/page_plan.c
    ui/menu_callbacks.c --> ui/pages/page_settings.c
    ui/menu_callbacks.c --> ui/pages/page_account.c
    ui/menu_callbacks.c --> ui/pages/page_word_manager.c

    ui/pages/page_home.c --> ui/raylib_word_ui.c
    ui/pages/page_home.c --> modules/fonts.c
    ui/pages/page_card_review.c --> modules/plan.c
    ui/pages/page_select_word.c --> modules/plan.c
    ui/pages/page_test.c --> modules/plan.c
    ui/pages/page_account.c --> modules/account.c
    ui/pages/page_plan.c --> modules/plan.c
    ui/pages/page_word_manager.c --> modules/words.c
    ui/pages/page_search.c --> modules/words.c
```

---

## 二、核心控制模块

### 2.1 全局状态管理器（core/app_state.c）

作为整个系统的**数据中枢**，`AppState` 采用单例模式将所有可变运行时状态统一管理。各业务模块通过依赖注入方式持有指向其子状态的指针，实现低耦合数据共享。

```mermaid
classDiagram
    class AppState {
        +UIStyle style
        +UIState uiState
        +bool isDarkMode
        +MENU* rootMenu
        +MENU* currentMenu
        +MenuStack menuStack
        +LearnState learn
        +ReviewState review
        +TestState test
        +SelectWordState selectWord
        +SearchState search
        +AccountState account
        +PlanState plan
        +bool loginRequired
        +Init()
        +Reset()
        +Deinit()
        +ToggleDarkMode()
    }

    class UIStyle {
        +UITheme theme
        +Font font
        +Font latinFont
        +float fontSize
        +float spacing
    }

    class UIState {
        +Vector2 mousePos
        +bool mouseDown
        +int hotItem
        +int activeItem
        +int focusItem
        +char keyboardText
    }

    class LearnState {
        +int currentWordIndex
        +bool showOnlyUnmastered
        +float scrollOffset
    }

    class ReviewState {
        +int reviewWords
        +int currentIndex
        +int sessionKnown
        +int sessionUnknown
        +bool sessionStarted
    }

    class TestState {
        +int questions
        +int currentIndex
        +int correctCount
        +bool done
    }

    AppState *-- UIStyle
    AppState *-- UIState
    AppState *-- LearnState
    AppState *-- ReviewState
    AppState *-- TestState
```

**设计优势：** 传统的面向对象做法会为每个对话框分配独立状态对象，导致跨页面状态共享困难。采用全局单例后，从"浏览学习"页面切换到"抽认卡"页面时，无需序列化/反序列化状态，只需切换导航指针即可。

### 2.2 树形菜单导航（core/tree_menu.c）

系统采用 **多路树数据结构** 组织页面导航。每个菜单节点持有一个 `show` 函数指针，运行时通过 `CURRENT_MENU->show()` 多态调用对应页面的渲染函数。

```mermaid
graph TD
    ROOT["根节点 (Root)"]
    ROOT --> N1["学单词<br/>MenuLearn_Show"]
    ROOT --> N2["背单词<br/>MenuReviewRoot_Show"]
    ROOT --> N3["查找单词<br/>MenuSearch_Show"]
    ROOT --> N4["学习计划<br/>MenuPlanRoot_Show"]
    ROOT --> N5["设置<br/>MenuSettings_Show"]
    ROOT --> N6["账号管理<br/>MenuAccount_Show"]
    ROOT --> N7["词库管理<br/>MenuWordManager_Show"]
    N2 --> N2A["卡片背单词<br/>MenuCardReview_Show"]
    N2 --> N2B["选词背单词<br/>MenuSelectWord_Show"]
    N2 --> N2C["测试<br/>MenuTest_Show"]
    N4 --> N4A["计划管理<br/>MenuPlanManager_Show"]
    N4 --> N4B["学习进度<br/>MenuProgress_Show"]

    style ROOT fill:#4a90d9,color:#fff
    style N1 fill:#67c23a,color:#fff
    style N2 fill:#e6a23c,color:#fff
    style N3 fill:#409eff,color:#fff
    style N4 fill:#f56c6c,color:#fff
    style N5 fill:#909399,color:#fff
    style N6 fill:#b882c0,color:#fff
    style N7 fill:#2f9e9e,color:#fff
```

**导航栈机制：**

```mermaid
flowchart LR
    subgraph 导航栈操作
        direction TB
        A[当前页面: 主页] -->|"用户点击 '卡片背单词'"| B["StackPush<br/>保存主页到栈<br/>切换currentMenu"]
        B --> C[当前页面: 卡片背单词]
        C -->|"用户点击 '返回'"| D["StackPop<br/>弹出栈顶<br/>恢复到主页"]
        D --> A
    end
```

数据结构定义：

```c
typedef struct menu {
    void (*show)(void);          // 页面渲染函数指针 —— 实现多态导航
    struct menu* child[8];       // 子节点数组，最大出度 8
    struct menu* parent;         // 父节点指针，支持回溯
    int childindex;              // 已挂载子节点计数
} MENU;

typedef struct {
    MENU* menuStack[10];         // 最大深度 10 层的导航栈
    int Stacktop;                // 栈顶指针，-1 表示空栈
} MenuStack;
```

---

## 三、业务逻辑模块

### 3.1 用户账户系统（modules/account.c）

实现多用户隔离的账户管理，支持注册、登录、注销与销户操作。密码采用 **djb2 哈希算法** 存储摘要，避免明文泄露风险。

```mermaid
flowchart TD
    A([程序启动]) --> B{accounts.txt存在?}
    B -->|否| C[创建默认管理员账户<br/>admin/admin]
    B -->|是| D[解析管道分隔字段<br/>加载至User数组]
    C --> E[初始化完成]
    D --> E

    F([用户登录请求]) --> G[遍历User数组比对用户名]
    G --> H{用户名存在?}
    H -->|否| I[返回错误: 用户不存在]
    H -->|是| J[djb2 Hash输入密码]
    J --> K{哈希值匹配?}
    K -->|否| L[返回错误: 密码错误]
    K -->|是| M[设置currentUserIndex<br/>切换至用户专属数据文件]
    M --> N[更新lastLoginTime<br/>持久化至accounts.txt]

    subgraph 数据文件隔离
        O["全局: progress.txt"]
        P["用户专属: progress_alice.txt"]
        Q["全局: plans.txt"]
        R["用户专属: plans_alice.txt"]
    end

    M --> P
    M --> R
```

**密码安全设计：** djb2 哈希算法虽非密码学安全级别（如 bcrypt），但相比明文存储，有效防止了直接查看配置文件导致的密码泄露。其轻量级特性避免了外部加密库依赖，适合本地桌面应用场景。

### 3.2 单词库引擎（modules/words.c）

实现单词数据的 CRUD 操作、无序索引至有序进度数组的映射、以及基于通配符的模糊搜索算法。

**数据映射流程：**

```mermaid
flowchart LR
    subgraph 存储层
        F1["words.txt<br/>word|phonetic|definition|example|translation"]
    end

    subgraph 索引层
        F2["WordEntry* g_wordLibrary[]<br/>动态数组，按文件顺序存储完整条目"]
    end

    subgraph 运行时层
        F3["WordWithProgress g_words[300]<br/>固定大小数组，携带进度元数据"]
    end

    F1 -->|"load_words_from_file()<br/>逐行解析管道分隔"| F2
    F2 -->|"init_words()<br/>浅拷贝指针 + 零初始化进度"| F3
    F3 -->|"load_progress()<br/>覆盖进度字段"| F3
    F3 -->|"save_progress()<br/>持久化进度至 progress.txt"| F4["progress.txt<br/>word|knownCount|unknownCount|lastReview"]
```

**通配符搜索算法：**

```mermaid
flowchart TD
    A([用户输入搜索模式]) --> B{模式中含有通配符?<br/>^ $ * ?}
    B -->|是| C["search_words_by_regex()<br/>自实现迷你正则引擎"]
    B -->|否| D["search_words_simple()<br/>不区分大小写子串匹配"]
    C --> E{匹配数 > 0?}
    E -->|是| F[返回通配符匹配结果]
    E -->|否| D
    D --> G[返回子串匹配结果]

    subgraph 支持的通配符语法
        H1["* → 零个或多个字符"]
        H2["? → 恰好一个字符"]
        H3["^ → 锚定开头"]
        H4["$ → 锚定结尾"]
    end
```

**检索示例：**
| 输入模式 | 匹配逻辑 | 示例匹配 |
|----------|---------|---------|
| `ab*` | 以 "ab" 开头 | abandon, abstract |
| `*tion` | 以 "tion" 结尾 | education, information |
| `h?t` | "h" + 任一字符 + "t" | hat, hit, hot |
| `^test` | 精确开头为 "test" | test, testing |

### 3.3 学习计划引擎（modules/plan.c）

实现自适应学习计划管理，支持计划的创建、激活与**跨日期自动推进**。

```mermaid
flowchart TD
    A([用户完成一个单词的学习]) --> B["Plan_AddStudiedToday(1)"]
    B --> C["Plan_CheckNewDay()"]
    C --> D["获取当前日期 yyyymmdd"]
    D --> E{与 lastStudyDate 比较}
    E -->|相同| F[studiedToday += 1]
    E -->|跨天| G["currentDay++<br/>studiedToday = 1<br/>lastStudyDate = today"]
    F --> H[更新 plans.txt]
    G --> H
    H --> I{studiedToday >= dailyWordCount?}
    I -->|是| J["今日目标已达成<br/>Plan_GetRemainingToday() = 0"]
    I -->|否| K["今日剩余: dailyWordCount - studiedToday"]
```

**四大预设学习方案：**

| 计划名称 | 每日单词量 | 总天数 | 累计覆盖 |
|---------|-----------|-------|---------|
| 一周入门计划 | 10 词/天 | 7 天 | 70 词 |
| 半月巩固计划 | 15 词/天 | 15 天 | 225 词 |
| 三十天进阶计划 | 20 词/天 | 30 天 | 600 词 |
| 六十天冲刺计划 | 30 词/天 | 60 天 | 1800 词 |

### 3.4 复习策略与进度追踪

系统采用**类间隔重复（Spaced Repetition）** 策略跟踪每个单词的学习状态。

```mermaid
flowchart TD
    A(["待复习单词池<br/>reviewWords[]"]) --> B{逐个展示抽认卡}
    B --> C["用户看到英文单词<br/>(卡片正面)"]
    C --> D["用户点击翻转<br/>看到中文释义<br/>(卡片背面)"]
    D --> E{用户判断}
    E -->|"认识 ✓"| F["knownCount++<br/>更新 lastReview"]
    E -->|"不认识 ✗"| G["unknownCount++<br/>更新 lastReview"]
    F --> H{knownCount >= 3?}
    H -->|是| I["标记为 mastered = true<br/>不再出现在待复习池"]
    H -->|否| J["保留在复习池<br/>下次继续出现"]
    G --> J
    I --> K([复习完成])
    J --> B

    subgraph 掌握度判别
        L["绿色: 掌握率 ≥ 80%"]
        M["橙色: 50% ≤ 掌握率 < 80%"]
        N["红色: 掌握率 < 50%"]
        O["掌握率 = knownCount / (knownCount + unknownCount)"]
    end
```

---

## 四、UI 基础组件层

### 4.1 即时模式 GUI 系统（ui/raylib_word_ui.c）

本系统采用**即时模式（Immediate Mode）** GUI 范式，区别于传统的保留模式（Retained Mode）。每个 UI 控件以函数的形式存在，在同一调用中同时完成输入处理与视觉渲染。

**即时模式核心原理：**

```mermaid
flowchart TD
    subgraph 保留模式 (Qt/GTK)
        R1[创建控件对象] --> R2[设置属性/回调]
        R2 --> R3[控件持有状态]
        R3 --> R4[事件循环驱动更新]
    end

    subgraph 即时模式 (本系统)
        I1["每帧调用 UIButton()"]
        I1 --> I2["检查鼠标是否在按钮区域"]
        I2 --> I3["更新 hotItem / activeItem"]
        I3 --> I4["根据状态选择颜色绘制"]
        I4 --> I5["返回是否被点击"]
    end
```

**热点系统状态机：**

```mermaid
stateDiagram-v2
    [*] --> Idle: 每帧开始
    Idle --> Hot: 鼠标进入控件区域
    Hot --> Active: 鼠标按下
    Active --> Hot: 鼠标松开(已完成一次点击)
    Hot --> Idle: 鼠标离开控件区域
    Active --> Active: 鼠标保持按下(拖拽)
    Active --> Idle: 鼠标在外部松开(取消)
```

**UI 主题系统：**

```mermaid
flowchart LR
    subgraph 浅色主题
        L1["背景: #F5F5F5"]
        L2["卡片: #FFFFFF"]
        L3["主文字: #333333"]
        L4["主色调: #4A90D9"]
        L5["成功色: #67C23A"]
        L6["危险色: #F56C6C"]
    end

    subgraph 深色主题
        D1["背景: #1E1E2E"]
        D2["卡片: #2D2D3F"]
        D3["主文字: #E0E0E0"]
        D4["主色调: #6CB4EE"]
        D5["成功色: #7ED321"]
        D6["危险色: #FF6B6B"]
    end

    L1 -->|"ToggleDarkMode()"| D1
    D1 -->|"ToggleDarkMode()"| L1
```

### 4.2 多语言文字渲染引擎（modules/fonts.c）

针对中英混排场景，实现了**基于 Unicode 码位的动态字体切换**渲染引擎。

```mermaid
flowchart TD
    A(["输入混合文本<br/>'abandon /əˈbændən/ 放弃'"]) --> B["逐字符遍历"]
    B --> C{Unicode 码位判断}
    C -->|"U+0000 ~ U+007F<br/>(ASCII 英文字母)"| D["使用英文字体渲染<br/>DejaVuSans.ttf"]
    C -->|"U+0080 ~ U+024F<br/>(拉丁扩展/IPA 音标)"| E["使用拉丁字体渲染<br/>DejaVuSans.ttf + IPA扩展码位"]
    C -->|"U+4E00 ~ U+9FFF<br/>(CJK 中文字符)"| F["使用合并字体渲染<br/>NotoSansCJK + 默认字体回退"]
    C -->|其他| F
    D --> G[累加字符宽度<br/>推进绘制坐标]
    E --> G
    F --> G
    G --> H{还有字符?}
    H -->|是| B
    H -->|否| I([渲染完成])
```

**字体后备链设计：**

```mermaid
flowchart LR
    A["首选: data/fonts/DejaVuSans.ttf"] -->|加载失败| B["Windows: C:/Windows/Fonts/arial.ttf"]
    B -->|加载失败| C["macOS: /System/Library/Fonts/Helvetica.ttc"]
    C -->|加载失败| D["Linux: /usr/share/fonts/truetype/dejavu/DejaVuSans.ttf"]
    D -->|加载失败| E["最终回退: GetFontDefault()<br/>raylib内置默认字体"]

    F["中文字体首选: data/fonts/NotoSansCJK.otf"] -->|加载失败| G["平台字体回退链..."]
```

### 4.3 抽认卡翻转动画

卡片背单词功能采用**三维仿射变换**模拟卡片翻转效果：

```mermaid
flowchart TD
    A([每帧调用 UIFlashCard]) --> B["计算翻转进度 t ∈ [0, 1]"]

    B --> C{t < 0.5?}
    C -->|是 卡片正面| D["scaleX = cos(t * PI)<br/>缩放因子: 1 → 0<br/>显示单词"]
    C -->|否 卡片背面| E["scaleX = cos(t * PI)<br/>缩放因子: 0 → -1<br/>显示释义+音标"]

    D --> F["DrawTextPro 渲染单词<br/>以卡片中心为锚点<br/>缩放 X 轴产生 3D 翻转感"]
    E --> F

    F --> G["底部: '认识' / '不认识' 按钮"]
    G --> H["用户操作后 t 重置<br/>下一单词出现"]
```

---

## 五、功能页面模块

### 5.1 页面职能矩阵

| 页面模块 | 核心功能 | 涉及的数据操作 | 学习计划联动 |
|---------|---------|--------------|------------|
| **主页仪表盘** | 全局统计概览 + 快捷入口 | 遍历 g_words[] 统计进度 | 读取活跃计划进度 |
| **浏览学习** | 逐词浏览，详情展示 | 读取 WordEntry 字段 | 无 |
| **卡片背单词** | 抽认卡翻转，认识/不认识判定 | 更新 WordProgress | Plan_AddStudiedToday(1) |
| **选词背单词** | 看中文释义选英文单词 | 更新 selectWordCorrect/Total | Plan_AddStudiedToday(1) |
| **测试模式** | 看英文单词选中文释义 | 仅本地计分 | Plan_AddStudiedToday(1) |
| **查找单词** | 通配符搜索 + 子串搜索 | 读取 g_wordLibrary | 无 |
| **学习计划** | 计划创建/激活/进度总览 | 读写 plans.txt | 无 |
| **系统设置** | 深浅主题切换 | 仅修改内存状态 | 无 |
| **账号管理** | 注册/登录/注销/销户 | 读写 accounts.txt | 文件路径重定向 |
| **词库管理** | 单词 CRUD 编辑 | 读写 words.txt | 保存时同步进度 |

### 5.2 典型学习流程

```mermaid
flowchart TD
    A([用户登录]) --> B[进入主页]
    B --> C{选择学习模式}

    C -->|浏览模式| D["逐词浏览单词<br/>查看释义和例句<br/>无进度记录"]
    D --> B

    C -->|抽认卡模式| E["系统筛选待复习单词<br/>reviewWords[]"]
    E --> F["翻转抽认卡<br/>认识 +1 / 不认识 +1"]
    F --> G{还有待复习?}
    G -->|是| F
    G -->|否| H["保存进度<br/>Plan_AddStudiedToday()"]
    H --> B

    C -->|选词模式| I["显示中文释义<br/>4选1 英文单词"]
    I --> J["视觉反馈: 绿正确/红错误"]
    J --> K{15题完成?}
    K -->|否| I
    K -->|是| L["累计正确率<br/>保存至账户"]
    L --> B

    C -->|测试模式| M["显示英文单词<br/>4选1 中文释义"]
    M --> N{10题完成?}
    N -->|否| M
    N -->|是| O["显示得分<br/>给出评价"]
    O --> B
```

---

## 六、数据持久化方案

### 6.1 文件存储设计

系统采用**管道分隔纯文本格式**作为存储方案，避免引入 SQLite 等外部依赖，确保可读性、可编辑性和零配置部署。

| 文件 | 字段结构 | 编码 |
|------|---------|------|
| `words.txt` | `word\|phonetic\|definition\|example\|translation` | UTF-8 |
| `accounts.txt` | `username\|passwordHash\|createdTime\|lastLoginTime\|correct\|total` | UTF-8 |
| `progress.txt` | `word\|knownCount\|unknownCount\|lastReview` | UTF-8 |
| `plans.txt` | `name\|dailyWordCount\|totalDays\|currentDay\|createdAt\|lastStudyDate\|studiedToday\|isActive` | UTF-8 |

### 6.2 多用户数据隔离方案

```mermaid
flowchart TD
    subgraph 未登录状态
        A["进度文件: progress.txt"]
        B["计划文件: plans.txt"]
    end

    subgraph 用户 Alice 登录后
        C["进度文件: progress_alice.txt"]
        D["计划文件: plans_alice.txt"]
    end

    subgraph 用户 Bob 登录后
        E["进度文件: progress_bob.txt"]
        F["计划文件: plans_bob.txt"]
    end

    A -->|"Account_Login('alice', ...)"| C
    C -->|"Account_Logout()"| A
    A -->|"Account_Login('bob', ...)"| E

    style A fill:#f9f,stroke:#333
    style C fill:#9f9,stroke:#333
    style E fill:#99f,stroke:#333
```

---

## 七、跨平台构建与发布

### 7.1 构建系统架构

```mermaid
flowchart LR
    subgraph 源代码
        S[src/*.c]
    end

    subgraph CMake构建系统
        CM["CMakeLists.txt<br/>C11 Standard"]
        CM -->|Unix| GCC["GCC/Clang<br/>-finput-charset=UTF-8"]
        CM -->|Windows MSVC| MSVC["MSVC<br/>/utf-8 flag"]
        CM -->|Windows MinGW| MINGW["MinGW-w64<br/>UCRT64"]
    end

    subgraph raylib链接策略
        RL{静态库 libraylib.a 存在?}
        RL -->|是| SL["静态链接<br/>零依赖可执行文件"]
        RL -->|否| DL["动态链接<br/>需附带 .so/.dll"]
    end

    S --> CM
    CM --> RL
```

### 7.2 CI/CD 自动发布管道

```mermaid
flowchart TD
    Push["推送 v* 标签"] --> Trig["触发 GitHub Actions"]

    Trig --> LinuxJob["Linux Job<br/>ubuntu-24.04"]
    Trig --> WinJob["Windows Job<br/>windows-2025 + MSYS2"]

    LinuxJob --> RaylibBuild["从源码编译 raylib 6.0<br/>静态链接"]
    RaylibBuild --> LinuxBuild["Ninja 编译项目"]
    LinuxBuild --> LinuxPkg["生成产物:<br/>1. AppImage<br/>2. tar.gz 便携版"]

    WinJob --> MSYS2Setup["MSYS2 UCRT64<br/>安装 gcc/cmake/ninja/raylib"]
    MSYS2Setup --> WinBuild["Ninja 编译项目"]
    WinBuild --> WinPkg["生成产物:<br/>zip 便携版<br/>含 exe + dll + data"]

    LinuxPkg --> Release["创建 GitHub Release"]
    WinPkg --> Release
    Release --> Done["发布三个文件:<br/>- 背单词软件-x86_64.AppImage<br/>- 背单词软件-linux-x86_64.tar.gz<br/>- 背单词软件-windows-x86_64.zip"]
```

---

## 八、关键技术决策与创新点

### 8.1 设计决策权衡

| 决策 | 选择 | 理由 | 代价 |
|------|------|------|------|
| GUI 范式 | 即时模式 | 无对象生命周期管理；代码量少 60%；状态完全显式 | 每帧重绘全部控件，CPU 消耗略高；长文本需每帧测量 |
| 数据存储 | 纯文本管道分隔 | 零依赖；用户可用记事本编辑；版本控制友好 | 无并发支持；无索引查询，大数据量下性能较差 |
| 语言 | C11 | 极致性能；极小二进制体积；与 raylib C API 无缝对接 | 无 STL，需手工管理字符串与动态数组 |
| 密码安全 | djb2 哈希 | 无外部加密库依赖；比明文安全 | 非密码学安全级别，不适用于网络传输场景 |
| 字体渲染 | 自研 DrawTextAuto | 完美解决中英混排问题；无额外库依赖 | 维护成本；字体后备链需针对各平台调优 |

### 8.2 创新亮点

1. **即时模式 GUI + 主题系统：** 在没有 GTK/Qt 等重型框架的情况下，从零实现了完整可用的图形界面组件库，支持深浅双主题一键切换。

2. **Unicode 级字体切换：** 针对 raylib 原生仅支持单一字体渲染的限制，设计了逐字符码位检测 + 动态字体切换算法，实现了 ASCII 英文、IPA 国际音标、CJK 中文字符在同一段落内的和谐共存。

3. **多用户隔离：** 通过文件路径重定向而非数据库模式字段实现了轻量级多用户数据隔离，每个用户的学习进度和计划完全独立。

4. **自适应学习计划：** 计划引擎自动感知日期跨越，无需用户手动签到即可实现 `currentDay` 推进与每日计数器重置。

---

## 九、总结

本系统以 C11 语言配合 raylib 图形库，实现了一款功能完备的跨平台英语单词记忆桌面软件。系统涵盖用户管理、单词库维护、四种学习模式、自适应计划管理等核心功能模块，通过即时模式 GUI 范式与 Unicode 级字体切换引擎，在轻量级的技术栈上实现了良好的用户体验。管道分隔纯文本文件作为数据持久化方案，确保了系统的零配置部署与高可维护性。项目通过 GitHub Actions 实现 Linux AppImage 与 Windows zip 双平台自动构建发布，具备完整的工程化交付能力。

---

## 附录 A：项目文件清单

```
Recite_English/
├── CMakeLists.txt                 # CMake 构建配置
├── build.sh                       # Linux 一键构建脚本
├── src/
│   ├── main.c                     # 程序入口 + 主循环
│   ├── core/
│   │   ├── config.h               # 全局常量定义
│   │   ├── app_state.c / .h       # 全局状态管理器
│   │   └── tree_menu.c / .h       # 树形菜单导航系统
│   ├── modules/
│   │   ├── account.c / .h         # 用户账户模块
│   │   ├── words.c / .h           # 单词库 + 进度 + 搜索
│   │   ├── plan.c / .h            # 学习计划模块
│   │   └── fonts.c / .h           # 多语言文字引擎
│   ├── ui/
│   │   ├── raylib_word_ui.c / .h  # 即时模式 GUI 组件库
│   │   ├── menu_callbacks.c / .h  # 菜单树初始化 + 导航栏
│   │   └── pages/
│   │       ├── pages.h            # 页面函数声明
│   │       ├── page_home.c        # 主页仪表盘
│   │       ├── page_learn.c       # 浏览学习
│   │       ├── page_review_root.c # 背单词入口
│   │       ├── page_card_review.c # 卡片背单词
│   │       ├── page_select_word.c # 选词背单词
│   │       ├── page_test.c        # 测试模式
│   │       ├── page_search.c      # 查找单词
│   │       ├── page_plan.c        # 学习计划管理
│   │       ├── page_settings.c    # 系统设置
│   │       ├── page_account.c     # 账号管理
│   │       └── page_word_manager.c # 词库管理
├── data/
│   ├── words.txt                  # 单词库数据
│   ├── accounts.txt               # 账户数据
│   └── fonts/                     # 字体文件
├── packaging/
│   └── AppImage/                  # AppImage 打包资源
└── .github/workflows/
    └── release.yml                # CI/CD 自动发布配置
```

## 附录 B：编译与运行

**Linux:**
```bash
./build.sh package    # 一键编译 + 打包
```

**Windows (MSYS2 UCRT64):**
```bash
mkdir build && cd build
cmake .. -G Ninja -DCMAKE_BUILD_TYPE=Release
ninja
```

**macOS:**
```bash
brew install raylib cmake ninja
mkdir build && cd build
cmake .. -G Ninja -DCMAKE_BUILD_TYPE=Release
ninja
```
