#!/bin/bash
# AppRun — AppImage 入口脚本
# cd 到 data/ 所在目录（程序用 ./data/ 相对路径读文件），再启动二进制

SELF_DIR="$(dirname "$(readlink -f "$0")")"
cd "$SELF_DIR/usr/share/recite-english" || exit 1
exec "$SELF_DIR/usr/bin/main_c" "$@"
